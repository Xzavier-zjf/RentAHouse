spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rental_platform?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  data:
    redis:
      host: localhost
      port: 6379

rental:
  jwt:
    secret: local-dev-rental-platform-secret-change-me
    expire-seconds: 7200

logging:
  level:
    root: INFO
    org.springframework.web: INFO

